# functions revisited
def exponential(number, exponent=2):
    result = number ** exponent
    return result

# create a function which prints but does not return anything

# call the function with simple values

# return a result then do something with it

# omit an argument
result = exponential(2, 4)
print(result)

# provide a default

# show using default

# override the default