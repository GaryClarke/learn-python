# create a variable which holds your first name
first_name = 'Gary'

# create a variable which holds your last name
last_name = 'Clarke'

# print(len(last_name))

# Concatenate your first_name and last_name and store in variable
full_name = first_name + ' ' + last_name
# Calculate length of full name using len() function
name_length = len(full_name)
# print out both full name and length of full name
print(full_name)
print(name_length)

# print full_name + name_length in one line e.g: Gary Clarke - 11
print(full_name + ' - ' + str(name_length))







